{
  "name": "functions",
  "private": true,
  "scripts": {
    "build": "tsc",
    "serve": "firebase emulators:start --only functions",
    "lint": "eslint . --ext .ts"
  },
  "dependencies": {
    "firebase-admin": "^11.14.0",
    "firebase-functions": "^4.6.0"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
